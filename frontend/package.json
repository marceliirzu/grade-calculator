{
  "name": "calcyourgpa-frontend",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "CalcYourGPA \u2014 GPA and grade calculator frontend (Vite + vanilla ES modules)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "doctor": "node ../scripts/doctor.mjs",
    "doctor:deploy": "node ../scripts/doctor.mjs --deploy"
  },
  "dependencies": {
    "@clerk/clerk-js": "^5.35.0"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "jsdom": "^25.0.1",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
