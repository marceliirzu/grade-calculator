// Environment configuration template
// Copy this file to env.js and fill in your values
// env.js is gitignored — never commit real values
window.ENV = {
    API_BASE_URL: 'https://YOUR_RAILWAY_BACKEND_URL/api'
};
